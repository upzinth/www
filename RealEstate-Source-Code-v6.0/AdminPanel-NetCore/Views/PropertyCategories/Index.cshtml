@model IEnumerable<RealEstate.Entities.PropertyCategory>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="d-flex flex-row align-items-center">
    <h3>
        Property Categories
    </h3>
    <a class="btn btn-info btn-sm ml-auto" asp-action="Create">Create Category</a>
</div>

<hr />

<table id="propertyCategoriesTable" class="table table-striped">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-info btn-sm"><span data-feather="edit-2"></span></a>
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm"><span data-feather="trash-2"></span></a>
            </td>
        </tr>
        }
    </tbody>
</table>

@section scripts {
    <script>
        $(document).ready(function () {
            createTable("#propertyCategoriesTable");
        });
    </script>
}